Put your own legally-owned audio file here, named exactly:

    our-song.mp3

The "Our Song" section on the site (أنت عمري) will load and play this file
once it exists. Nothing is bundled here on purpose — the site was built to
respect copyright, so no copy of the song is downloaded or included automatically.

Notes:
- The player will NOT autoplay. Sara (or Mahmoud) has to press Play.
- If the file is missing, the player will simply show a small hint instead
  of playing — nothing will break.
- Any standard .mp3 file works. Keep the filename exactly "our-song.mp3",
  or update the src="audio/our-song.mp3" reference in public/index.html
  if you'd rather use a different filename.
